{% extends 'base.html.twig' %}

{% block title %}Mofification de l'événement
{% endblock %}

{% block body %}
	<div class="container">
		<div class="form-container container_edit">
			<div class="form">
				<h1>Mofification de l'événement</h1>

				{{ include('evenement/_form.html.twig', {'button_label': 'Modifier'}) }}

				{{ include('evenement/_delete_form.html.twig') }}
				<div class="loginAncre">
					<a href="{{ path('app_evenement_index') }}">Retour à la liste</a>
				</div>

			</div>
		</div>
	</div>
{% endblock %}
